name: Build

on:
  push:
    branches: [main]
  pull_request:

env:
  # 4.6.3 is the minimum engine for Google Play's target-API-36 deadline
  # (2026-08-31): its android template ships targetSdk/compileSdk 36 AND
  # the API-36 predictive-back fix (GH-117653) that keeps the in-game
  # Back stack working on Android 16. Bump all three values together,
  # and keep the SDK packages below in sync with the template's
  # config.gradle (buildTools / ndkVersion).
  GODOT_VERSION: "4.6.3"
  GODOT_RELEASE: "4.6.3-stable"
  # Godot installs templates under <version>.<channel> (with a dot), but
  # release URLs use <version>-<channel> (with a hyphen). Keep both.
  GODOT_TEMPLATE_DIR: "4.6.3.stable"

jobs:
  test:
    name: Headless GUT tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Godot binary
        id: cache-godot
        uses: actions/cache@v4
        with:
          path: /usr/local/bin/godot
          key: godot-${{ env.GODOT_RELEASE }}-linux

      - name: Install Godot ${{ env.GODOT_RELEASE }}
        if: steps.cache-godot.outputs.cache-hit != 'true'
        run: |
          set -euxo pipefail
          wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
          unzip -q "Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
          sudo mv "Godot_v${GODOT_RELEASE}_linux.x86_64" /usr/local/bin/godot
          sudo chmod +x /usr/local/bin/godot

      - name: Import project (populate class_name registry)
        run: |
          godot --headless --path . --import 2>&1 | tail -20 || true

      - name: Run GUT
        # The tee (no pipefail) deliberately masks GUT's exit code — it's
        # noisy (benign headless-shutdown errors can force nonzero on a
        # fully-passing run, see DEV_NOTES). The NEXT step is the gate,
        # judging by parsed JUnit results instead.
        run: |
          godot --headless --path . -s addons/gut/gut_cmdln.gd \
            -gdir=res://game/tests/ -ginclude_subdirs -gexit \
            -gjunit_xml_file=res://gut_results.xml 2>&1 | tee gut_output.log

      - name: Assert suite integrity (no silent skips, no failures)
        # GUT SILENTLY SKIPS test scripts that fail to parse — it logs a
        # warning and still prints "All tests passed" (bit us in v0.15.17:
        # a helper-name collision with GutTest._summary skipped a whole
        # file). Three independent guards:
        #   1. no skip/parse-error lines in the run output;
        #   2. JUnit XML reports failures="0";
        #   3. the XML testcase count >= the static count of `func test_`
        #      in game/tests — a skipped file makes runtime < static.
        run: |
          set -euo pipefail
          if grep -E "Ignoring script|Failed to load script|Parse error" gut_output.log; then
            echo "::error::GUT skipped or failed to load at least one test script (see lines above)"
            exit 1
          fi
          FAILURES=$(grep -o 'failures="[0-9]*"' gut_results.xml | head -1 | grep -o '[0-9]*')
          RUNTIME=$(grep -c "<testcase" gut_results.xml)
          STATIC=$(grep -rhE "^func test_" game/tests --include="*.gd" | wc -l)
          echo "failures=$FAILURES runtime_tests=$RUNTIME static_tests=$STATIC"
          if [ "$FAILURES" != "0" ]; then
            # NAME the failures as ::error:: annotations, not just the count.
            # Job logs need repo auth to read, but annotations are public via
            # the check-runs API — without this a CI-only failure (one that
            # does not reproduce on a contributor's engine build) is a bare
            # "1 failing test" with no way to find out which. Bit us on
            # v0.15.22, where the suite was green locally on 4.6.1/4.7.x and
            # red on CI's pinned 4.6.3.
            #
            # awk, not python: the godot-ci container is not guaranteed to
            # ship an interpreter. Rules run in order, so a <failure> on the
            # same line as its <testcase> still picks up the name first.
            awk '
              /<testcase/ {
                if (match($0, /name="[^"]*"/)) tname=substr($0, RSTART+6, RLENGTH-7);
                if (match($0, /classname="[^"]*"/)) tclass=substr($0, RSTART+11, RLENGTH-12);
              }
              /<failure|<error/ {
                if (tname != "") {
                  printf "::error::FAILING TEST: %s :: %s\n", tclass, tname;
                  tname="";
                }
              }
            ' gut_results.xml
            # Full blocks to the log for whoever can read it.
            echo "--- failing testcase detail ---"
            grep -A6 "<failure" gut_results.xml || true
            echo "::error::GUT reported $FAILURES failing test(s)"
            exit 1
          fi
          if [ "$RUNTIME" -lt "$STATIC" ]; then
            echo "::error::Only $RUNTIME tests ran but $STATIC are declared — a test script was silently skipped"
            exit 1
          fi

  build-windows:
    name: Windows export (debug)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Godot binary
        uses: actions/cache@v4
        with:
          path: /usr/local/bin/godot
          key: godot-${{ env.GODOT_RELEASE }}-linux

      - name: Cache export templates
        uses: actions/cache@v4
        with:
          path: ~/.local/share/godot/export_templates/${{ env.GODOT_TEMPLATE_DIR }}
          key: godot-templates-${{ env.GODOT_RELEASE }}

      - name: Ensure Godot binary
        run: |
          set -euxo pipefail
          if [ ! -x /usr/local/bin/godot ]; then
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
            unzip -q "Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
            sudo mv "Godot_v${GODOT_RELEASE}_linux.x86_64" /usr/local/bin/godot
            sudo chmod +x /usr/local/bin/godot
          fi

      - name: Install export templates
        run: |
          set -euxo pipefail
          if [ ! -d "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}" ]; then
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_export_templates.tpz"
            mkdir -p "$HOME/.local/share/godot/export_templates"
            unzip -q "Godot_v${GODOT_RELEASE}_export_templates.tpz" -d /tmp/templates
            mv /tmp/templates/templates "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}"
          fi

      - name: Import project
        run: godot --headless --path . --import 2>&1 | tail -10 || true

      - name: Export Windows Desktop (debug)
        run: |
          mkdir -p exports/windows
          godot --headless --path . --export-debug "Windows Desktop" exports/windows/IdleBeastPractices.exe

      - uses: actions/upload-artifact@v4
        with:
          name: windows-debug
          path: exports/windows

  build-web:
    name: Web export (debug)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Godot binary
        uses: actions/cache@v4
        with:
          path: /usr/local/bin/godot
          key: godot-${{ env.GODOT_RELEASE }}-linux

      - name: Cache export templates
        uses: actions/cache@v4
        with:
          path: ~/.local/share/godot/export_templates/${{ env.GODOT_TEMPLATE_DIR }}
          key: godot-templates-${{ env.GODOT_RELEASE }}

      - name: Ensure Godot binary
        run: |
          set -euxo pipefail
          if [ ! -x /usr/local/bin/godot ]; then
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
            unzip -q "Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
            sudo mv "Godot_v${GODOT_RELEASE}_linux.x86_64" /usr/local/bin/godot
            sudo chmod +x /usr/local/bin/godot
          fi

      - name: Install export templates
        run: |
          set -euxo pipefail
          if [ ! -d "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}" ]; then
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_export_templates.tpz"
            mkdir -p "$HOME/.local/share/godot/export_templates"
            unzip -q "Godot_v${GODOT_RELEASE}_export_templates.tpz" -d /tmp/templates
            mv /tmp/templates/templates "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}"
          fi

      - name: Import project
        run: godot --headless --path . --import 2>&1 | tail -10 || true

      - name: Export Web (debug)
        run: |
          mkdir -p exports/web
          godot --headless --path . --export-debug "Web" exports/web/index.html

      - uses: actions/upload-artifact@v4
        with:
          name: web-debug
          path: exports/web

  build-android-debug:
    name: Android debug APK
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          # Match Godot 4.6.3's platform/android/java/app/config.gradle:
          # compileSdk 36, buildTools 36.1.0, ndkVersion 29.0.14206865.
          # AGP can auto-download missing pieces, but pre-installing keeps
          # the gradle step fast and the versions explicit.
          packages: "platform-tools platforms;android-36 build-tools;36.1.0 ndk;29.0.14206865"

      - name: Cache Godot binary
        uses: actions/cache@v4
        with:
          path: /usr/local/bin/godot
          key: godot-${{ env.GODOT_RELEASE }}-linux

      - name: Cache export templates
        uses: actions/cache@v4
        with:
          path: ~/.local/share/godot/export_templates/${{ env.GODOT_TEMPLATE_DIR }}
          key: godot-templates-${{ env.GODOT_RELEASE }}

      - name: Ensure Godot binary
        run: |
          set -euxo pipefail
          if [ ! -x /usr/local/bin/godot ]; then
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
            unzip -q "Godot_v${GODOT_RELEASE}_linux.x86_64.zip"
            sudo mv "Godot_v${GODOT_RELEASE}_linux.x86_64" /usr/local/bin/godot
            sudo chmod +x /usr/local/bin/godot
          fi

      - name: Install export templates
        run: |
          set -euxo pipefail
          if [ ! -d "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}" ]; then
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_RELEASE}/Godot_v${GODOT_RELEASE}_export_templates.tpz"
            mkdir -p "$HOME/.local/share/godot/export_templates"
            unzip -q "Godot_v${GODOT_RELEASE}_export_templates.tpz" -d /tmp/templates
            mv /tmp/templates/templates "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}"
          fi

      - name: Generate debug keystore
        run: |
          keytool -genkey -v -keystore debug.keystore \
            -alias androiddebugkey -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US" \
            -storepass android -keypass android

      - name: Install Android gradle build template
        # export_presets.cfg has gradle_build=true so Godot needs the
        # android_source.zip extracted into the project's android/build/
        # AND an android/.build_version marker (the GUI's Install Android
        # Build Template writes this; without it, export fails with "no
        # version info for it exists").
        #
        # Also append a gradle hook to patch the merged manifest's
        # resizeableActivity attribute to "false". See release.yml's
        # equivalent step for the full rationale (foldable orientation
        # lock); short version: Godot hardcodes resizeableActivity=true
        # which makes Android 12+ large screens ignore screenOrientation.
        run: |
          mkdir -p android/build
          unzip -q "$HOME/.local/share/godot/export_templates/${GODOT_TEMPLATE_DIR}/android_source.zip" -d android/build
          echo "${GODOT_TEMPLATE_DIR}" > android/.build_version
          cat >> android/build/build.gradle <<'EOF'

          afterEvaluate {
              tasks.matching { it.name ==~ /process.*Manifest.*/ }.all { Task t ->
                  t.doLast {
                      project.fileTree(dir: project.buildDir, include: '**/AndroidManifest.xml').each { File f ->
                          String text = f.text
                          if (text.contains('android:resizeableActivity="true"')) {
                              f.text = text.replace('android:resizeableActivity="true"', 'android:resizeableActivity="false"')
                              println "[patch] resizeableActivity=false in ${f.canonicalPath}"
                          }
                      }
                  }
              }
          }
          EOF

      - name: Write stub Play Games Services games_strings.xml
        # The PGS plugin injects a manifest meta-data tag referencing
        # @string/game_services_project_id. Even debug builds need that
        # resource to exist or AAPT errors out. Use a numeric stub since
        # debug builds aren't shipping cloud sync — only release.yml
        # injects the real PGS_APP_ID from the secret.
        run: |
          mkdir -p android/build/src/main/res/values
          cat > android/build/src/main/res/values/games_strings.xml <<'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <string name="game_services_project_id" translatable="false">0</string>
          </resources>
          EOF

      - name: Import project
        run: godot --headless --path . --import 2>&1 | tail -10 || true

      - name: Export Android (debug)
        # Godot 4.6 reads keystore env vars directly; no editor_settings.tres
        # write needed.
        env:
          GODOT_ANDROID_KEYSTORE_DEBUG_PATH: ${{ github.workspace }}/debug.keystore
          GODOT_ANDROID_KEYSTORE_DEBUG_USER: androiddebugkey
          GODOT_ANDROID_KEYSTORE_DEBUG_PASSWORD: android
        run: |
          mkdir -p exports/android
          godot --headless --path . --export-debug "Android" exports/android/IdleBeastPractices-debug.aab

      - name: Assert manifest targetSdk >= 36 + resizeableActivity=false
        # Two silent-failure classes this pins:
        # 1. targetSdkVersion — Play rejects updates targeting < 36 after
        #    2026-08-31, and the value is inherited from the engine's
        #    template default (export_presets.cfg leaves target_sdk blank),
        #    so an engine downgrade would silently ship 35 again.
        # 2. resizeableActivity="false" — applied by a gradle doLast hook
        #    that rewrites the merged manifest (see the template-install
        #    step); if Godot's exporter changes shape the hook stops
        #    matching and only prints nothing. Assert the result instead.
        run: |
          set -euo pipefail
          wget -q https://github.com/google/bundletool/releases/download/1.17.2/bundletool-all-1.17.2.jar -O bundletool.jar
          MANIFEST=$(java -jar bundletool.jar dump manifest --bundle=exports/android/IdleBeastPractices-debug.aab)
          # `|| true`: a no-match grep must fall through to the guard below
          # (set -e would otherwise kill the step before the ::error line).
          TARGET=$(echo "$MANIFEST" | grep -o 'targetSdkVersion="[0-9]*"' | head -1 | grep -o '[0-9]*' || true)
          echo "targetSdkVersion=${TARGET:-<none>}"
          if [ -z "$TARGET" ] || [ "$TARGET" -lt 36 ]; then
            echo "::error::AAB targets SDK ${TARGET:-<none>}; Google Play requires >= 36 from 2026-08-31"
            exit 1
          fi
          if ! echo "$MANIFEST" | grep -q 'resizeableActivity="false"'; then
            echo "::error::resizeableActivity=false patch did not apply — large-screen orientation control is broken (see DEV_NOTES.md)"
            exit 1
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: android-debug
          path: exports/android
