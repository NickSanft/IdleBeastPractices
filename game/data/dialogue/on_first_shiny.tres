[gd_resource type="Resource" script_class="DialogueLineResource" load_steps=2 format=3]

[ext_resource type="Script" path="res://game/resources/dialogue_line_resource.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"first_shiny"
trigger_id = &"on_first_shiny"
text = "A shiny variant. Note the iridescence. The Synod will require you to log the precise hue, because we are tedious in a deliberate, principled fashion."
mood = &"reverent"
weight = 1.0
max_uses = 1
